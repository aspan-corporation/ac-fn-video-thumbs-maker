{
  "name": "ac-fn-video-thumbs-maker",
  "version": "0.1.0",
  "bin": {
    "ac-fn-video-thumbs-maker": "bin/ac-fn-video-thumbs-maker.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "synth": "npm run build && cdk synth",
    "deploy": "npm run build && cdk deploy --require-approval never",
    "deploy:with-approval": "npm run build && cdk deploy",
    "diff": "npm run build && cdk diff",
    "destroy": "cdk destroy",
    "bootstrap": "cdk bootstrap"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.159",
    "@types/jest": "^29.5.14",
    "@types/node": "^24.10.1",
    "aws-cdk": "2.1100.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "@aspan-corporation/ac-cdk-constructs": "file:../ac-cdk-constructs",
    "aws-cdk-lib": "2.232.1",
    "constructs": "^10.0.0"
  }
}
